socket_tun_path(/dev/net/tun);
socket_type(UDP);
socket_use_encryption(1);
socket_server_addr(127.0.0.1);
socket_server_port(8000);

iface_dev(tvpnc0);
iface_override_default(1);

auth_username(public_user);
auth_password(public_user_password);
